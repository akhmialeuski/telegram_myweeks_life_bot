"""Network integration tests package.

This package contains integration tests that require network access
to interact with the Telegram API via Local Bot API Server.

These tests are marked with @pytest.mark.network and @pytest.mark.req.
Run them separately: pytest tests/integration/network/ -m network
"""
